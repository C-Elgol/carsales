services:
  docs:
    image: carsales_local_docs
    container_name: carsales_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./carsales:/app/carsales:z
    ports:
      - '9000:9000'
    command: /start-docs
